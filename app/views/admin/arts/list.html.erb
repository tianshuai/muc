<%=provide(:title, '后台首页')%>
  <span class="blank10"></span>

	<%=render 'admin/base/left_bar'%>
	
    <div class="span10">
      <!--Body content-->

          <%= render "admin/categories/tool_bar"%>
          <span class="blank10"></span>
          <div class="sub_operation clearfix">
             <div class="left_operation">
                选择：
                <a href="#" id="select_all">全选 </a>
                ｜
                <a href="#" id="select_un">反选 </a>
                ｜
                <a href="#" id="select_cancel">取消 </a>
              </div>
                  <div class="clearfix">
                    <div class="operate_box clearfix">
                        <%=link_to('+新建', '#', :class=>'select_post')%>
                        <%=link_to('审核通过', '#', :class=>'select_post')%>
                      <%=link_to('禁用', '#', :class=>"select_post")%>
                    </div>
                  </div>
          </div>
          <span class="blank10"></span>
          <table class="admin-box">
            <thead>
              <tr>
                <th></th>
                <th>ID</th>
                <th>名称</th>
                <th>标识</th>
                <th>类型</th>
                <th>排序</th>
                <th>状态</th>
                <th>管理</th>
              </tr>
            </thead>
            <tbody >
              <%@categories.each do |b|%>
                <%= render partial: "admin/categories/item", :locals => { :b => b } %>
              <%end%>
            </tbody>
          </table>
          <%= will_paginate @categories if @categories.present? %>
        </div>
    </div>

    </div>
  </div>

